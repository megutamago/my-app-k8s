include /etc/keepalived/argocd.conf
# include /etc/keepalived/grafana.conf
# include /etc/keepalived/prometheus.conf
# include /etc/keepalived/minio.conf
# include /etc/keepalived/loki.conf
# include /etc/keepalived/tempo.conf

# Define the script used to check if haproxy is still working
vrrp_script chk_haproxy { 
    script "/usr/bin/killall -0 haproxy"
    interval 2 
    weight 2 
}
