apiVersion: v1
kind: Service
metadata:
  name: argocd-server-lbn
  namespace: argocd
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: argocd
    app.kubernetes.io/name: argocd-server
  ports:
  - port: 443
    targetPort: server
    nodePort: 31000
    protocol: TCP